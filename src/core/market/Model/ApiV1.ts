import kernelapi from "@/base/api/kernelapi"
import { schema, common ,model} from '@/base';
export {schema,common,model}
/**
 * 为模型提供能力扩展
 * 注意：能力扩展不能有实例，此为抽象类
 */
export default class ApiV1 extends kernelapi{
    
}